# Protected A5 legacy compatibility fixtures

These fixtures reproduce only the legacy JSON **shape** established by Goal
075: an absolute historical `goal_ref` and a JSON-null `budget_state_ref`.
They do not copy the retained lease bytes or production evidence. The real 059
lease digest is retained only as a historical identifier in the sanitized model.
