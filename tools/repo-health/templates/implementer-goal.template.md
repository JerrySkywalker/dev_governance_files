# Repository-health Implementer Goal

One writer root owns every authorized repository write. Use at most seven direct read-only subagents, never recurse, and admit a repository only after AGENTS.md, branch-policy, and preservation-rule checks pass.
