# Repository-health Supervisor Goal

The supervisor is product-repository read-only. It performs architect-first blocker analysis, never mutates Git, and may generate only the next bounded Goal plus governance state.
